steps:
  - name: gcr.io/cloud-builders/go
    args:
      - build
      - -ldflags
      - '-w'
      - '-o'
      - bin/gateway-$TAG_NAME-linux-amd64
      - ./cmd/gateway
    env:
      - 'GOOS=linux'
      - 'GOARCH=amd64'
  - name: gcr.io/cloud-builders/gsutil
    args:
      - cp
      - bin/*
      - gs://brickd-artifacts