steps:
  - name: golang
    args:
      - build
      - -ldflags
      - '-w'
      - '-o'
      - bin/gateway-$TAG_NAME-linux-arm
      - ./cmd/gateway
    env:
      - 'GO111MODULE=on'
      - 'GOOS=linux'
      - 'GOARCH=arm'
      - 'GOARM=5'
  - name: gcr.io/cloud-builders/gsutil
    args:
      - cp
      - bin/*
      - gs://brickd-artifacts